package main

import (
	"fmt"
	"os"
)

// Placeholder until services are implemented in later stages.
func main() {
	fmt.Fprintln(os.Stderr, "scaffold placeholder — implement in later stages")
	os.Exit(0)
}
